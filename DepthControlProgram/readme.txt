1. x轴定义在车辆前进方向（横滚角），z轴定义为垂直地面向上（自旋角），y轴垂直另外两轴的平面（俯仰角）
2. 输入参数为（耕深，机具类型，土壤硬度，作业速度，地形坡度），输出参数为三点悬挂高度
3.流程如下：
(1).首先开始实验训练模型，pda会下发三点悬挂高度H实际高度，模型会根据耕深算出三点悬挂高度H预测。通过H实际高度和H预测，来在线学习，训练模型的准确率。
(2).然后开始第二次实验，pda会下发目标耕深target。如果当前程序测量出的耕深和目标耕深target不同，要根据目标耕深target来反推出三点悬挂高度H预测，并发送给pda，让农机调整实际的三点悬挂高度。
(3).最后查看日志，和测量实际耕深效果。检验实验效果。

4.指令说明
指令为（发送当前机具类别：旋耕机）（数据区0xA5 0x07 0x21 0x01 0x4A 0xB9 0xAA）其中4a b9是根据0xA5 0x07 0x21 0x01CRC16Modbus得出，0xAA是尾部，A5是头部，07是长度。
然后完整指令为0x50 0x06 0xA5 0x07 0x21 0x01 0x4A 0xB9 0xAA 0xED 0xA6，新增的50 06是固定的，ed a6是根据0x50 0x06 0xA5 0x07 0x21 0x01 0x4A 0xB9 0xAA得到的CRC校验，规则与前面相同。


修改任务（在新增几何关系计算耕深的基础上）：
1. 增加pda下发目标耕深、实际三点悬挂高度、作业速度，三个指令下发的接收。已完成
2. 增加pda下发控制指令后，客户端才上行数据（包括耕深、耕深稳定性等）。已完成
3. 增加RLS在线学习模型，以根据目标耕深预测三点悬挂高度，模型训练流程如上所示。已完成
4. 当前日志信息不完整，无法有效评估实验效果，无法量化训练阶段的模型收敛情况。已完成
5. 模型参数未持久化，重启后丢失训练成果，需要自动保存模型文件并加载已有模型。已完成
6. 土壤硬度参数固定不变，模型适应性差。模型特征包含 深度×硬度 交互项，但硬度不变时该项退化为 常数×深度
7. 控制模式下高度指令发送频率过高。增加指令发送的去抖或最小间隔（例如 0.5~1 秒），或仅在耕深与目标偏差超过阈值时发送。
8. 当前指令的接收会丢包。已完成
9. 控制模式下发送的指令语义错误（致命缺陷）,已完成
10.控制为纯开环，无耕深闭环修正，已完成
11.训练与控制模式未隔离，互相干扰，已完成